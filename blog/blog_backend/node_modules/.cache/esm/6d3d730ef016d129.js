let mongoose;_f01‍.x([["default",()=>_f01‍.o]]);_f01‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);

const { Schema} = mongoose;

const PostSchema = new Schema({
    title: String,
    body : String,
    tags: [String], // 문자로 이루어진 배열
    publishedDate : {
        type : Date,
        default : Date.now // 현재 날짜를 기본값으로 지정
    }
})

const Post = mongoose.model('Post',PostSchema);
_f01‍.d(Post);